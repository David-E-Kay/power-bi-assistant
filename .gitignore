# OneDrive / Windows artifacts
desktop.ini
Thumbs.db

# Machine-local Claude Code config (keep .claude/skills/ — it's project content)
.claude/settings.local.json
# per-user MCP server paths — not tied to the project
.claude/.mcp.json

# Python
__pycache__/
*.pyc

# Session deliverables — kept on disk locally, never published
output/

# Graphify local knowledge graph outputs
graphify-out/

# NuGet-provisioned DLLs (downloaded at runtime by provision_libs.py)
libs/

# Generated model-schema snapshots (regenerable cache; keep the explanatory README)
artifacts/model-schema/model-schema-*.md
