# OneDrive / Windows artifacts
desktop.ini
Thumbs.db

# Machine-local Claude Code config (keep .claude/skills/ — it's project content)
.claude/settings.local.json
# per-user MCP server paths — not tied to the project
.claude/.mcp.json

# Graphify skill + its local auto-invoke trigger — kept on disk, excluded from the public repo
.claude/skills/graphify/
.claude/CLAUDE.md

# Python
__pycache__/
*.pyc

# Session deliverables — kept on disk locally, never published
output/

# Graphify local knowledge graph outputs
graphify-out/

# NuGet-provisioned DLLs (downloaded at runtime by provision_libs.py)
libs/

# Generated model-schema snapshots (regenerable cache; keep the explanatory README)
artifacts/model-schema/model-schema-*.md

# Local MCP server packages
.mcp-servers/
analysis-services.powerbi-modeling-mcp-*.vsix
.mcp.json

# Power BI work created in this project but not part of the Assistant repo
scripts/install_tabular_editor_script_library_te2.csx
tests/test_te_script_installer.py
